# Asset Drop Zone

This folder is a temporary drop zone.
Place all raw screenshots, JPGs, PNGs, certificates, and videos here.
The import script (\`import-assets.js\`) will later optimize, rename, and move them into their permanent locations.

**Original files placed here will remain untouched.**
